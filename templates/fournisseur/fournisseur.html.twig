<div class="container-fluid contenu">

    {# -- formulaire fournisseur -- #}
    {{ form_start(form) }}

        <div class="row">
            <div class="col-md-7">{{ form_row(form.nom) }}</div>
        </div>

        <div class="row">
            <div class="col-md-3">{{ form_row(form.adresse) }}</div>
            <div class="col-md-3">{{ form_row(form.mail) }}</div>
            <div class="col-md-3">{{ form_row(form.telephone) }}</div>
        </div>

        <div class="row">
            <div class="col-md-7">{{ form_row(form.nomContact) }}</div>
            <div class="col-md-7">{{ form_row(form.dateRelation) }}</div>
        </div>

        <div class="row">
            <div>{{ form_rest(form) }}</div>
        </div>

        <div class="row">
            <div class="col-md-3"></div>
            <button class="btn btn-primary btn-xs"><i class="fa fa-save"></i> {{ button|default('Enregistrer') }}</button>
            <div class="col-md-1"></div>
            <button class="btn btn-info btn-xs" type="reset" title="Effacer la saisie"><i class="fa fa-eraser"> Annuler</i></button>
            <div class="col-md-1"></div>
            <a href="{{ path('fournisseur')}}" class="btn btn-secondary btn-xs"><i class="fa fa-undo"></i> Retour liste</a></span></th>
        </div>

    {{ form_end(form) }}

</div>
