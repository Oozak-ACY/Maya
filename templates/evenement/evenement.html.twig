<div class="container-fluid contenu">

    {# -- formulaire evenement -- #}
    {{ form_start(form) }}

        <div class="row">
            <div class="col-md-7">{{ form_row(form.titre) }}</div>
            <div class="col-md-5">{{ form_row(form.description) }}</div>
        </div>

        <div class="row">
            <div class="col-md-3">{{ form_row(form.date) }}</div>
            <div class="col-md-3">{{ form_row(form.horaire) }}</div>
        </div>

        <div class="row">
            <div>{{ form_rest(form) }}</div>
        </div>

        <div class="row">
            <div class="col-md-3"></div>
            <button class="btn btn-primary btn-xs"><i class="fa fa-save"></i> {{ button|default('Enregistrer') }}</button>
            <div class="col-md-1"></div>
            <button class="btn btn-info btn-xs" type="reset" title="Effacer la saisie"><i class="fa fa-eraser"> Annuler</i></button>
            <div class="col-md-1"></div>
            <a href="{{ path('evenement')}}" class="btn btn-secondary btn-xs"><i class="fa fa-undo"></i> Retour liste</a></span></th>
        </div>

    {{ form_end(form) }}

</div>
